# Evolution: 2026-05-22 15:57

## Intelligence Summary
- **Patches analyzed:** 2 critical session patches (Main-thread event loop panic & Cyrillic/Unicode output typing corruption)
- **Recurring patterns:**
  - GUI Event Loop Thread-Safety: Winit/eframe requires taking over the main thread; launching it from background thread causes panics.
  - Keyboard Emulation: `enigo.text()` corrupts Cyrillic and Uzbek character inputs on Windows when typing longer sequences.
- **Tech stack:** Rust, egui + eframe, tray-icon, global-hotkey, cpal, hound, ureq, enigo, arboard.

## Improvements Applied

### `egui-tray-application`
- **lifecycle:** Added strict warning requiring `eframe::run_native` to run exclusively on the main thread to prevent winit panics on Windows.
- **emulation:** Added the Unicode-safe Clipboard-Paste fallback pattern under `6. Output Processors`, deprecating raw `enigo.text()` typing for multi-byte Unicode/non-ASCII text insertion.

### `aif-review`
- **checklist:** Appended new items to `Correctness` checklist to review that GUI event loops run exclusively on the main thread and that emulated typing handles multi-byte/non-ASCII Unicode safely.

## Patterns Identified
- **Winit event loop threading panic**: 1 occurrence (fixed in `src/main.rs`)
- **Non-ASCII character typing corruption**: 1 occurrence (fixed in `src/output.rs` via Clipboard-Paste fallback)
